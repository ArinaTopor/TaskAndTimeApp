export interface IProject {
    id: string;
    title: string;
    color?: string;
}

export interface ISection {
    id: string;
    title: string;
}
export interface ITodo {
    id: string;
    name: string;
}
